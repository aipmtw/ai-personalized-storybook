# Luce 繪本誌 — 產品規格書

> app.markluce.ai · 兒童雙語繪本月刊訂閱服務（3-12 歲）

## 訂閱方案

| 項目 | 說明 |
|------|------|
| 價格 | NT$99/月 |
| 內容 | 全年齡層（3-6、7-9、10-12 歲），當月 + 上月所有繪本 |
| 閱讀方式 | 線上閱讀（瀏覽器）+ PWA 離線下載 |
| 試閱（未登入） | 每本書前 3 頁免費，無需登入 |
| 試閱（LINE 登入） | 每本書前 6 頁免費（3 頁匿名 + 3 頁登入加碼） |
| 付款方式 | ECPay 定期定額扣款 |
| 取消訂閱 | 隨時取消，存取權限持續至帳期結束 |

### 內容存取層級

| 層級 | 存取範圍 | 頁數 | 登入 |
|------|---------|------|------|
| 未登入 | 封面 + 第 1-3 頁 | 3 頁免費 | 否 |
| LINE 登入（免費） | 封面 + 第 1-6 頁 | 6 頁免費 | 是 |
| 訂閱者（NT$99/月） | 全部頁面，當月 + 上月 | 無限制 | 是 |
| 過期月份 | 未來進階方案 | 待定 | 是 |

### 轉換漏斗
1. **未登入** → 讀 3 頁 → 閘門 → 「用 LINE 登入解鎖更多」
2. **LINE 登入（免費）** → 讀 6 頁 → 訂閱閘門 → 「訂閱解鎖完整繪本 NT$99/月」
3. **訂閱者** → 完整存取、PWA 下載、離線閱讀

### 離線與到期政策

**原則：已下載 = 永久保留。新內容需要有效訂閱。**

| 狀態 | 線上閱讀 | 離線（已下載繪本） |
|------|---------|------------------|
| 有效訂閱者 | 當月 + 上月全部 | 所有已下載繪本自由播放 |
| 到期 < 30 天 | 閘門在第 6 頁（登入層級） | 已下載繪本仍可播放 |
| 到期 > 30 天 | 閘門在第 3 頁（匿名層級） | 已下載繪本仍可播放，顯示柔性橫幅：「訂閱已過期 — 重新訂閱解鎖新繪本」 |
| 從未訂閱 | 閘門在第 3/6 頁 | 不適用（下載需有效訂閱） |

**為何允許到期後離線閱讀：**
- 小孩不懂帳單 — 在飛機上突然被鎖是很糟糕的體驗
- 內容為自製，無授權壓力需撤回
- 寬鬆政策建立信任，提高續訂率
- 檔案已在裝置上 — 人為 DRM 是敵意的使用者體驗

**柔性橫幅（非封鎖）：**
- 訂閱到期超過 30 天時，於 App 啟動時顯示
- 「你的訂閱已過期。已下載的繪本仍可閱讀。重新訂閱即可取得最新繪本！」
- 關閉按鈕 + 訂閱 CTA
- 不會阻擋閱讀已下載內容

## 身份驗證

- **登入方式**：LINE Login（LIFF v2.1、OIDC）
- **不支援 Google 登入**（設計決策 — 台灣市場，LINE 優先）
- **Token**：Base64 編碼 JSON，包含 `userId`、`displayName`、`exp`
- **Cookie**：`ml_auth` 用於 session 持久化
- **離線驗證**：已下載的 PWA 繪本需一次性登入啟用；下次連線時 token 自動更新
- **管理員存取**：僅限 Mark 的 LINE 用戶 ID

## 閱讀器功能（shared/reader.js v3）

### 頁面模型
```
第 0 頁    = 封面      URL: /slug/       無音檔
第 1..N 頁 = 內容      URL: /slug/1      音檔: page-{N}-{lang}.mp3
結尾       = 通用      URL: /slug/end    無音檔，恭喜 + CTA
```

- 頁碼在閱讀器 URL、管理後台、音檔之間完全一致
- 無偏移：`/amy/1` = 管理後台第 1 頁 = `page-1-zh.mp3`

### 封面頁
- 書名（中 + EN）、作者、版本
- 統計列：頁數、中/英音檔數量（透過 HEAD 探測）
- **[開始閱讀 Start Reading ▶]** 按鈕 → 跳至第 1 頁 + 自動播放音檔
- 所有元素遵循語言選擇器

### 內容頁
- Emoji 插圖 + 雙語文字（中文 + English）
- **[下一頁 Next Page ▶]** 按鈕在底部 → 翻頁 + 自動播放音檔
- 最後一頁顯示 **[故事結束 The End ▶]** → 跳至 `/slug/end`
- 下一頁按鈕下方顯示台北時間
- 文字可見性由語言選擇器控制（中/英/雙語）

### 結尾畫面（`/slug/end`）
- 🎉 恭喜訊息：「恭喜你讀完了！Congratulations!」
- 書名 + 副標回顧
- 頁數回顧：「你剛讀完 N 頁的精彩故事！」
- **[📚 探索更多繪本 ▶]** — 顯眼綠色 CTA 連結書架
- 作者署名 + 版本
- 全面雙語，遵循語言選擇器

### 語言選擇器
- 三種模式：中文 / English / 中＋EN
- 儲存於 `localStorage`
- 控制可見性：
  - 封面：書名、副標、作者、統計、開始閱讀按鈕
  - 內容：text-zh / text-en 區塊
  - 結尾畫面：恭喜、回顧、CTA、署名
  - 自動播放按鈕：自動讀整本書 / Auto-Read Book
  - UI 元素使用 `i18n-zh`/`i18n-en`/`i18n-both` class（與內容 `text-zh`/`text-en` 分離）

### 導航
- 工具列圓點指示器：📖（封面）+ N 個內容圓點，目前頁面以青色高亮
- 左右滑動、點擊邊緣（25%/75%）、鍵盤方向鍵
- 點擊 📖 圓點回到封面
- 結尾畫面的上一頁/下一頁正常運作

### 音檔
- 逐頁 TTS 音檔：`page-{N}-zh.mp3`、`page-{N}-en.mp3`
- Azure Neural TTS：zh-TW-HsiaoChenNeural、en-US-JennyNeural
- 音檔控制列：播放/暫停、進度條、時間顯示、關閉
- 開始閱讀和下一頁按鈕自動播放
- **[▶▶ 自動讀整本書 / Auto-Read Book]**：朗讀整本書，自動翻頁，結束時顯示結尾畫面

### 身份驗證與試閱閘門（雙層）
- **未登入**：封面 + 第 1-3 頁免費 → 第 4 頁觸發閘門
  - 閘門彈窗：「試閱結束 — 用 LINE 登入解鎖更多頁面」
  - LINE 登入按鈕 + 「返回封面」連結
- **LINE 登入（免費，未訂閱）**：封面 + 第 1-6 頁免費 → 第 7 頁觸發閘門
  - 閘門彈窗：「免費試閱結束 — 訂閱 NT$99/月 解鎖完整繪本」
  - 訂閱按鈕 + 「返回封面」連結
- **訂閱者**：全部頁面，無閘門
- 登入後返回使用者嘗試存取的頁面
- 右上角顯示登入狀態：用戶名（青色）或「未登入」（淡色）

### 工具列
- 🏠 首頁連結
- 🔊 音檔播放按鈕
- 圓點導航（📖 + 內容圓點）
- ⛶ 全螢幕切換
- 版本號（右對齊，低調顯示）

### PWA 與離線

**安裝方式：單一 App 級 PWA** — 使用者在 `app.markluce.ai` 安裝一次「Luce 繪本誌」，而非 36 個獨立繪本 App。

| 層級 | 範圍 | 策略 | 快取內容 |
|------|------|------|---------|
| App SW（`/sw.js`） | 整個 App | HTML、共用 JS 使用 network-first | 書架、shared/reader.js |
| 繪本 SW（`/{slug}/sw.js`） | 單本繪本 | HTML + /shared/ 使用 network-first，素材使用 cache-first | 繪本 HTML、CSS、data.js、音檔 MP3 |

- 根目錄 manifest：「Luce 繪本誌」含 App 圖示，`start_url: "/"`
- 各繪本 manifest 保留用於個別繪本中繼資料
- 快取版本控制強制更新
- 已下載繪本需一次性線上驗證，之後可離線播放

**自動升級流程：**
1. SW 偵測到新版本（快取名稱變更）
2. 新 SW 在背景安裝（`skipWaiting`）
3. 啟用時顯示提示：「新版本已準備好 / New version ready — 點擊更新 / Tap to update」
4. 使用者點擊 → 頁面重新載入使用新版本
5. 使用者忽略 → 下次自然載入頁面時使用新版本（透過 `clients.claim`）
6. 提示遵循語言選擇器

## 網域架構

| 網域 | 用途 | 程式碼庫 |
|------|------|---------|
| markluce.ai | 行銷網站、API、ESL、專題、每日簡報 | markluce-ai |
| app.markluce.ai | 繪本 App（閱讀器 + 管理後台 + 書架） | ai-personalized-storybook |

**管理後台留在 app.markluce.ai**（不移至 markluce.ai/app/admin）。原因：
- 同源存取繪本 data.js 與音檔 MP3 — 無 CORS 問題
- 同一程式碼庫、同一部署 — 無跨庫建置複雜度
- 共用 LINE Login 驗證流程 — 相同 cookies、相同 session
- 慣例：管理後台與其管理的 App 在同一網域
- 更簡潔的 URL：`/admin/amy` vs `/app/admin/amy`

## 管理後台（app.markluce.ai/admin/）

### 總覽（`/admin/`）
- LINE 身份驗證（僅限管理員）
- 當期控制（設定上線月份）
- 繪本表格：編號、slug、中/英書名、年齡層、頁數、預估中/英/雙語時長、音檔狀態、月份、狀態
- 篩選：狀態、年齡層、月份、排序、搜尋
- 內容規格參考表

### 繪本詳情（`/admin/{slug}`）
- 總覽表格：封面（無編號）+ 內容頁（1-N）+ 結尾列
- 逐頁：類型標籤、文字預覽（可點擊）、字數、中/英 MP3 實際時長、Emoji
- 透過 `AudioContext.decodeAudioData` 探測音檔（可靠、逐一）
- 音檔摘要：檔案數、總時長、缺漏警示
- 點擊列 → 詳情視圖

### 封面預覽（`/admin/{slug}/cover`）
- 閱讀器封面視覺模擬（Emoji、書名、統計、開始閱讀按鈕）
- 繪本資訊標籤
- 探測後音檔摘要

### 頁面詳情（`/admin/{slug}/{N}`）
- 視覺預覽（Emoji + 完整文字）
- 文字分析：字數/詞數 vs 年齡層規格
- 內嵌 `<audio>` 播放器，含中/英 MP3 時長
- 上一頁/下一頁導航 + 在閱讀器中開啟

### 結尾預覽（`/admin/{slug}/end`）
- 閱讀器結尾畫面視覺模擬
- 資訊標籤

## 繪本資料架構

### 每本書檔案（18 本使用新閱讀器）
```
public/{slug}/
├── index.html          ← 引用 shared/reader.js?v=5
├── css/style.css       ← 每本書獨特主題
├── js/data.js          ← BOOK 物件：slug、title、pages[]
├── audio/              ← page-{N}-{zh,en}.mp3
├── manifest.json       ← PWA manifest
└── sw.js               ← service worker（v4，/shared/ 使用 network-first）
```

### 共用檔案
```
public/shared/
└── reader.js           ← 通用閱讀器邏輯（單一來源）
```

### data.js 頁面類型
- `type: 'cover'` — 固定為 `pages[0]`，無文字，無音檔
- `type: 'story'` — 內容頁，含中/英文字 + Emoji
- （不再有 `type: 'end'` — 已移除，通用結尾由 reader.js 渲染）

### 各年齡層規格
| 年齡層 | 頁數/本 | 字/頁 | 詞/頁 | TTS 語速 |
|--------|---------|-------|-------|----------|
| 3-6 歲 | 12 | 20-40 | 15-30 | 中 -20%、英 -25% |
| 7-9 歲 | 16 | 40-70 | 30-50 | 中 -10%、英 -15% |
| 10-12 歲 | 20 | 60-100 | 50-100 | 中 0%、英 -5% |

## 法律頁面

### 服務條款（`/terms/`）
- 服務：雙語繪本月刊訂閱
- 價格：NT$99/月，透過 ECPay 定期定額
- 內容存取：當月 + 上月，全年齡層
- 免費試閱：每本書前 3 頁，無需帳號；LINE 登入後前 6 頁
- 僅限 LINE 登入，每個 LINE 用戶一個帳號
- 允許 PWA 下載，離線閱讀需初次驗證
- 內容由 AI 生成，經人工編輯審閱

### 隱私權政策（`/privacy/`）
- 蒐集資料：LINE 個人資料（用戶 ID、顯示名稱、大頭貼）
- 閱讀分析：瀏覽頁面、音檔播放事件、裝置資訊
- 兒童隱私：不直接蒐集兒童資料；家長的 LINE 帳號為訂閱者
- 付款：完全由 ECPay 處理，不儲存卡片資料
- 除 ECPay 計費外，不與第三方分享資料
- 資料刪除：透過電子郵件申請，30 天內處理

### 退款政策（`/refund/`）
- 數位內容：存取後一般不予退款
- 首月保證：首次訂閱 7 天內可全額退款
- 取消訂閱：於當期帳期結束時生效，不提供部分退款
- 技術問題：個案補償（延長存取或退款）

### 關於（`/about/`）
- Luce 繪本誌 by MarkLuce.ai
- 兒童雙語繪本月刊（3-12 歲）
- AI 生成故事，人工審閱
- 台灣製造，繁體中文 + English

### 聯繫
- Email：mark@aipm.com.tw（頁尾 mailto 連結，無獨立頁面）

## 已修復 Bug（2026-04-16）

1. **音檔偏移一頁**：封面播放第 1 頁音檔，內容頁全部偏移 1
2. **36 份相同 reader.js**：整合為單一 `/shared/reader.js`
3. **頁碼不一致**：閱讀器/管理後台/音檔現在使用相同的 0 索引系統
4. **雙語模式隱藏文字**：i18n span 與內容 div 的 CSS class 衝突
5. **封面作者署名非雙語**：現在遵循語言選擇器
6. **無法返回封面**：圓點導航含 📖 封面圖示
7. **結尾頁為故事內容**：最後一頁故事與通用結尾畫面分離
8. **音檔探測卡住**：從 Audio 元素切換至 AudioContext.decodeAudioData
9. **SW 快取過期 reader.js**：/shared/ 檔案使用 network-first
10. **下一頁未自動播放音檔**：使用 goToPageAndPlay + 正確時序修復

## 新增功能（2026-04-16）

1. **共用 reader.js** — 18 本書的單一來源
2. **管理後台繪本詳情**（`/admin/{slug}`）— 逐頁 MP3 時長探測
3. **管理後台封面預覽**（`/admin/{slug}/cover`）
4. **管理後台頁面詳情**（`/admin/{slug}/{N}`）— 完整文字、音檔播放器、規格檢查
5. **管理後台結尾預覽**（`/admin/{slug}/end`）
6. **封面統計列** — 頁數 + 音檔數量（透過 HEAD 探測）
7. **[開始閱讀] 按鈕**含自動播放
8. **[下一頁] 按鈕**含自動播放
9. **圓點導航**取代文字頁碼指示器
10. **通用結尾畫面**含恭喜、回顧、書架 CTA
11. **`/slug/end` URL** — 語意化結尾 URL
12. **語言選擇器** — 全部 UI 元素遵循中/英/雙語
13. **自動播放重新命名** — 自動讀整本書 / Auto-Read Book
14. **登入狀態** — 右上角顯示（用戶名或未登入）
15. **工具列版本號** — 右對齊低調顯示
16. **台北時間** — 下一頁按鈕下方顯示
